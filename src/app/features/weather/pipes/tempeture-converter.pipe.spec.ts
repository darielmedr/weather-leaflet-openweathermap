import { TempetureConverterPipe } from './tempeture-converter.pipe';

describe('TempetureConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TempetureConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
